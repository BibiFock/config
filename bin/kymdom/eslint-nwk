#!/usr/bin/env bash

package=$(echo $1 | sed 's/.*\/packages\/\([^\/]*\)\/.*$/\1/g')
file=$(echo $1 | sed "s/.*\/packages\/\([^\/]*\)\///g")

cd $(npm root)/../packages/$package

../../node_modules/eslint/bin/eslint.js -f json --stdin --stdin-filename $file
