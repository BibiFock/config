[user]
    name = bbr
    email = bj@webedia.fr

[color]
    ui = auto

    [color "branch"]
        current = yellow reverse
        local = yellow
        remote = green
    [color "diff"]
        meta = yellow bold
        frag = magenta bold
        old = red bold
        new = green bold
        whitespace = red reverse
    [color "status"]
        added = yellow
        changed = green
        untracked = cyan

[core]
    whitespace=fix,-indent-with-non-tab,trailing-space,cr-at-eol
    excludesfile = /home/bbr/.gitignore_global
	editor = vim

[alias]
    st = status
    ci = commit
    br = branch
    co = checkout
    df = diff
    lg = log -p
    unstage = reset HEAD --
    last = log -1 HEAD
    lol = log --pretty=format:\"%C(green)%cd %C(red bold)%an%Creset %C(yellow)%h %C(white)%s %C(cyan)%d\" --graph --date-order --date=relative -20
    loldf = log --pretty=format:\"%C(green)%cd %C(red bold)%an%Creset %C(yellow)%h %C(white)%s %C(cyan)%d\" --graph --date-order --date=relative -10 --decorate --numstat --shortstat
    loa = log --pretty=format:\"%C(green)%cd %C(red bold)%an%Creset %C(yellow)%h %C(white)%s %C(cyan)%d\" --graph --date-order --date=iso --all
    loadf = log --pretty=format:\"%C(green)%cd %C(red bold)%an%Creset %C(yellow)%h %C(white)%s %C(cyan)%d\" --graph --date-order --date=iso --all --decorate --numstat --shortstat
    ls = ls-files
    d = difftool
    hist = whatchanged -p 
    dfl = diff --word-diff=porcelain
    #uncommit
    unci = reset --soft 'HEAD^'

[mergetool "fugitive"]
    cmd = vim -f -c \"Gdiff\" \"$MERGED\"
[merge]
    tool = fugitive
[push]
	default = current
