#scripts
wbdb -> check catalog db state << IMPORTANT >>

#vim
split function params when to long, automaticly go to next line (,,) = (,\n ,\n)

#minux
trouver nouveau wm i3wm
trouver nouveau term

#git
check zsh shortcut
check how add autocomplete to sco

#admin
(mod199)handle product rank: alert message for product enrich, no enrich product on two first lines
(mod100) msg prochain publish (list files) + explication délai de publication
(mod100) possibility reset value of a field

#fun
car race
uke online free editor (http://ukegeeks.com/)
bd stream (poulpy)

